(in-package :turtl)

(defun decode-key (auth-key)
  "Given a JSON/Base64 auth key, turn it into a binary key."
  auth-key)

(defun encode-key (auth-key)
  "Given a binary auth key, turn it into a JSON/Base64 key."
  auth-key)

